fixture_rule_one:
  user: one
  account: expense_account
  match_pattern: FIXTURE_PATTERN_ONE
  match_type: contains
  priority: 0

fixture_rule_two:
  user: one
  account: expense_account
  match_pattern: FIXTURE_PATTERN_TWO
  match_type: exact
  priority: 5

fixture_rule_exclude:
  user: one
  match_pattern: FIXTURE_EXCLUDE_PATTERN
  match_type: contains
  exclude: true
  priority: 8
