<%= form_with(model: transaction) do |form| %>
  <% if transaction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(transaction.errors.count, "error") %> prohibited this transaction from being saved:</h2>

      <ul>
        <% transaction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :transacted_at, style: "display: block" %>
    <%= form.datetime_select :transacted_at %>
  </div>

  <div data-controller="category-picker">
    <%= form.label :category_name, "Category", style: "display: block" %>
    <%= form.select :_category_select,
          options_for_select(
            [["— None —", ""]] +
            current_user.categories.order(:name).map { |c| [c.name, c.id] } +
            [["+ New category…", "__new__"]],
            transaction.category_id
          ),
          {},
          data: { category_picker_target: "select", action: "change->category-picker#changed" } %>
    <div class="category-new-input" style="display:none" data-category-picker-target="newWrapper">
      <%= form.text_field :_category_new,
            value: "",
            placeholder: "New category name",
            data: { category_picker_target: "textInput", action: "input->category-picker#typed" } %>
      <a href="#" class="category-new-cancel" data-action="click->category-picker#back" title="Back to list">&times;</a>
    </div>
    <%= form.hidden_field :category_id, data: { category_picker_target: "idField" } %>
    <%= form.hidden_field :category_name, value: "", data: { category_picker_target: "nameField" } %>
  </div>

  <div>
    <%= form.label :src_account_id, "Source Account" , style: "display: block" %>
    <%= form.select :src_account_id, current_user.accounts.sourceable.collect { |a| [a.name, a.id] } %>
  </div>

  <div>
    <%= form.label :dest_account_id, "Destination Account", style: "display: block" %>
    <%= form.select :dest_account_id, current_user.accounts.destinable.collect { |a| [a.name, a.id] } %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :amount_minor, style: "display: block" %>
    <%= form.text_field :amount_minor %>
  </div>


  <div>
    <%= form.label :fx_amount_minor, style: "display: block" %>
    <%= form.text_field :fx_amount_minor %>
  </div>

  <div>
    <%= form.label :fx_currency_id, style: "display: block" %>
    <%= form.select :fx_currency_id, Currency.all.collect { |c| [c.code, c.id] }, include_blank: true %>
  </div>

  <div>
    <%= form.label :notes, style: "display: block" %>
    <%= form.text_area :notes %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
